# Bayes, fuzzy, greylist and rate-limit state. This is the mail host's own
# Redis — never the application's: a flushed cache on the web side must not be
# able to erase the spam filter's memory.
servers = "127.0.0.1:6379";
timeout = 1s;
